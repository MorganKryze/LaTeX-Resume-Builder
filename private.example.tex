% =============================================================
% private.tex — LOCAL-ONLY personal fields (NOT committed).
%
% Copy this file to `private.tex` (next to your resume .tex source)
% and uncomment/fill in the fields you want. The class loads it via
% `\InputIfFileExists{private.tex}{}{}`, after declaring empty defaults.
%
% Keep `private.tex` git-ignored. It is never pushed, never published
% to gh-pages, and never seen by CI — so the public build renders
% WITHOUT these fields, while a local `make build` picks them up.
% With no private.tex present, the class defaults (empty) apply.
%
% Overleaf: upload `private.tex` alongside the other sources to get a
% private build there too.
%
% Add more fields: declare an empty default in resume.cls
% (`\newcommand{\resumeXxx}{}`), render it where you want, then set it
% here with `\renewcommand`.
% =============================================================

% --- Phone number ----------------------------------------
% Shown in the header contact row, between the email and LinkedIn.
% Underlined to match the other links, but not hyperlinked, so a value
% with spaces stays clean. Wrap it in \href{tel:...} if you want a
% clickable link (use a space-free number for that).
% \renewcommand{\resumePhone}{+33 6 12 34 56 78}
